import { Box } from '@material-ui/core';
import React from 'react'

const Content = () => (
    <Box display='flex' justifyContent='center' border={1} height='90vh' width='70%'>
        Despliegue de nodos
    </Box>
)

export default Content;