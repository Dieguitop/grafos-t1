import React from "react";

// TODO: implementar.
export default function MinimumSpanningTree() {
  return (
    <>
      <div className="main_contentInside">
        {arbolGenerador && <Content className="hola" data={rows} linksData={arbolGenerador} />}
      </div>
    </>
  );
}
